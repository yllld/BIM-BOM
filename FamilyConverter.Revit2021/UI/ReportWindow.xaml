<Window x:Class="FamilyConverter.Revit2021.UI.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DWG Converter - Family Geometry: отчет"
        Width="960"
        Height="660"
        MinWidth="820"
        MinHeight="540"
        Background="#F7F6FA"
        FontFamily="Segoe UI"
        FontSize="13"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="DwgConverterTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <DockPanel Style="{StaticResource DwgRootDockStyle}">
        <Border DockPanel.Dock="Top" Style="{StaticResource DwgTopBarStyle}">
            <StackPanel>
                <TextBlock Text="Отчет конвертации" Style="{StaticResource DwgHeaderTitleTextStyle}" />
                <TextBlock x:Name="SummaryText" Style="{StaticResource DwgHeaderSubtitleTextStyle}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Border DockPanel.Dock="Bottom" Style="{StaticResource DwgFooterBarStyle}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Закрыть"
                        Width="112"
                        Style="{StaticResource DwgPrimaryButtonStyle}"
                        IsDefault="True"
                        Click="CloseButton_Click" />
            </StackPanel>
        </Border>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource DwgSectionCardStyle}">
                <StackPanel>
                    <TextBlock x:Name="ReportPathsText" TextWrapping="Wrap" Foreground="{StaticResource DwgTextBrush}" />
                    <TextBlock x:Name="MessagesText" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{StaticResource DwgMutedTextBrush}" />
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource DwgCardStyle}" Padding="0">
                <DataGrid x:Name="ResultsGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Object" Binding="{Binding ObjectId}" Width="90" />
                        <DataGridTextColumn Header="Layer" Binding="{Binding LayerName}" Width="130" />
                        <DataGridTextColumn Header="Geometry" Binding="{Binding SourceGeometryType}" Width="90" />
                        <DataGridTextColumn Header="Class" Binding="{Binding LocalClassification}" Width="95" />
                        <DataGridTextColumn Header="Conf." Binding="{Binding LocalConfidence, StringFormat=0.00}" Width="70" />
                        <DataGridTextColumn Header="Method" Binding="{Binding FinalMethod}" Width="90" />
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90" />
                        <DataGridCheckBoxColumn Header="Fallback" Binding="{Binding FallbackUsed}" Width="75" />
                        <DataGridTextColumn Header="ElementId" Binding="{Binding CreatedElementId.IntegerValue}" Width="80" />
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </DockPanel>
</Window>
